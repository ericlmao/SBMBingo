package games.negative.bingo.api.model.goal;

public enum BingoGoalType {

    // Collect Item (pickup item / mine / craft / smelt)
    COLLECT,

    // Kill Entity (kill entity / kill player)
    KILL,

    // Get Potion Effect (get potion effect)
    POTION_EFFECT,

    // Die (die)
    DIE,

    // Find Biome (find a biome)
    FIND_BIOME,
}
